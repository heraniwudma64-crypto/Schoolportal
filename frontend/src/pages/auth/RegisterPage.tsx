import { useEffect, useState } from 'react';
import { api } from '../../lib/api';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';
import { ArrowRight, GraduationCap, Loader2 } from 'lucide-react';
import { Toaster, toast } from 'sonner';
import { APP_DESCRIPTION } from '../../config/branding';

type FormData = Record<string, string>;
const initial: FormData = { role: 'student', name: '', idNumber: '', email: '', password: '', confirmPassword: '', grade: '', department: '', institutionId: '', institutionName: '', admissionType: '', fatherName: '', grandfatherName: '', gender: '', dob: '', nationality: '', disability: 'no', disabilityType: '', guardianFullName: '', familyHeadGender: '', guardianEmail: '', guardianPhone: '', parentStatus: '', fatherEducationLevel: '', motherEducationLevel: '', economicStatus: '', familyKebele: '', locationType: '', residenceRegion: '', residenceZone: '', residenceWoreda: '', birthRegion: '', birthZone: '', birthWoreda: '', nationalId: '' };
const fields: Array<[string, string, string?]> = [
  ['institutionId', 'Institution ID'], ['institutionName', 'Institution Name'], ['idNumber', 'Student ID'], ['admissionType', 'Admission Type'], ['name', 'First Name'], ['fatherName', "Father's Name"], ['grandfatherName', "Grandfather's Name"], ['nationality', 'Nationality'], ['guardianFullName', "Parent's or Guardian's Full Name"], ['guardianEmail', "Parent's or Guardian's Email", 'email'], ['guardianPhone', "Parent's or Guardian's Phone", 'tel'], ['parentStatus', 'Parent Status'], ['fatherEducationLevel', "Father's or Male Guardian's Education Level"], ['motherEducationLevel', "Mother's or Female Guardian's Education Level"], ['economicStatus', 'Student Economic Status'], ['familyKebele', 'Family Kebele'], ['locationType', 'Location Type'], ['residenceRegion', 'Region of Residence'], ['residenceZone', 'Zone of Residence'], ['residenceWoreda', 'Woreda of Residence'], ['birthRegion', 'Region of Birth'], ['birthZone', 'Zone of Birth'], ['birthWoreda', 'Woreda of Birth'], ['nationalId', 'National ID'],
];

export default function RegisterPage() {
  const [form, setForm] = useState(initial); const [isLoading, setIsLoading] = useState(false);
  const { user } = useAuth(); const navigate = useNavigate();
  useEffect(() => { if (user) navigate('/dashboard', { replace: true }); }, [user, navigate]);
  const change = (event: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => setForm(current => ({ ...current, [event.target.name]: event.target.value }));
  const input = (name: string, label: string, type = 'text', required = true) => <label className="block text-sm font-medium text-gray-700">{label}{required && <span className="text-red-600"> *</span>}<input name={name} type={type} required={required} value={form[name]} onChange={change} className="mt-1 w-full rounded-lg border border-gray-200 px-3 py-2 focus:border-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-100" /></label>;
  const section = (title: string, children: React.ReactNode) => <fieldset className="rounded-xl border border-gray-200 p-4"><legend className="px-2 font-semibold text-blue-950">{title}</legend><div className="grid gap-4 sm:grid-cols-2">{children}</div></fieldset>;
  const submit = async (event: React.FormEvent) => { event.preventDefault(); if (form.password !== form.confirmPassword) return toast.error('Passwords do not match.'); if (form.role === 'student' && form.disability === 'yes' && !form.disabilityType.trim()) return toast.error('Disability type is required when disability is Yes.'); if (form.role === 'student' && !/^[+0-9()\-\s]{7,25}$/.test(form.guardianPhone)) return toast.error('Enter a valid parent or guardian phone number.'); setIsLoading(true); try { await api.post('/auth/register', { ...form, name: form.name.trim(), idNumber: form.idNumber.trim(), email: form.email.trim() || undefined, role: form.role.toUpperCase(), disabilityType: form.disability === 'yes' ? form.disabilityType.trim() : undefined }); toast.success('Account created successfully. Please sign in.'); navigate('/login', { replace: true }); } catch (error: any) { toast.error(error.response?.data?.message || 'Registration failed.'); } finally { setIsLoading(false); } };
  const student = form.role === 'student';
  return <div className="min-h-screen bg-gradient-to-br from-blue-950 via-blue-800 to-indigo-900 px-4 py-10"><main className="mx-auto max-w-5xl rounded-2xl bg-white p-6 shadow-2xl sm:p-8"><header className="mb-7 text-center"><GraduationCap className="mx-auto mb-3 h-12 w-12 text-blue-900" /><h1 className="text-2xl font-bold text-gray-900">Create Account</h1><p className="text-sm text-gray-500">{APP_DESCRIPTION}</p></header><form onSubmit={submit} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2">{input('email', 'Account Email', 'email', false)}<label className="block text-sm font-medium text-gray-700">Account type<select name="role" value={form.role} onChange={change} className="mt-1 w-full rounded-lg border border-gray-200 px-3 py-2"><option value="student">Student</option><option value="parent">Parent</option><option value="teacher">Staff / Teacher</option></select></label></div>{student ? <>{section('A — Institution / Academic Information', <>{fields.slice(0, 4).map(([key, label, type]) => input(key, label, type))}{input('grade', 'Class / Grade')}</>)}{section('B — Personal Information', <>{fields.slice(4, 8).map(([key, label, type]) => input(key, label, type))}<label className="block text-sm font-medium text-gray-700">Sex <span className="text-red-600">*</span><select required name="gender" value={form.gender} onChange={change} className="mt-1 w-full rounded-lg border border-gray-200 px-3 py-2"><option value="">Select sex</option><option value="Male">Male</option><option value="Female">Female</option></select></label>{input('dob', 'Date of Birth', 'date')}</>)}{section('C — Disability Information', <><label className="block text-sm font-medium text-gray-700">Disability <span className="text-red-600">*</span><span className="mt-2 flex gap-4 font-normal"><span><input type="radio" name="disability" value="yes" checked={form.disability === 'yes'} onChange={change} /> Yes</span><span><input type="radio" name="disability" value="no" checked={form.disability === 'no'} onChange={change} /> No</span></span></label>{input('disabilityType', 'Disability Type', 'text', form.disability === 'yes')}</>)}{section('D — Family / Guardian Information', <>{fields.slice(8, 16).map(([key, label, type]) => input(key, label, type))}<label className="block text-sm font-medium text-gray-700">Family Head's Gender <span className="text-red-600">*</span><select required name="familyHeadGender" value={form.familyHeadGender} onChange={change} className="mt-1 w-full rounded-lg border border-gray-200 px-3 py-2"><option value="">Select gender</option><option value="Male">Male</option><option value="Female">Female</option></select></label></>)}{section('E — Residence Information', fields.slice(16, 21).map(([key, label, type]) => input(key, label, type)))}{section('F — Birth Location', fields.slice(21, 24).map(([key, label, type]) => input(key, label, type)))}{section('G — National Identification', input('nationalId', 'National ID'))}</> : <>{input('name', 'Full Name')}{input('idNumber', 'ID Number')}{form.role === 'teacher' ? input('department', 'Subject / Department') : input('grade', 'Child Class / Grade', 'text', false)}</>}<div className="grid gap-4 sm:grid-cols-2">{input('password', 'Password', 'password')}{input('confirmPassword', 'Confirm Password', 'password')}</div><button disabled={isLoading} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-blue-900 py-3 font-semibold text-white disabled:opacity-70">{isLoading ? <Loader2 className="h-5 w-5 animate-spin" /> : <>Create Account <ArrowRight className="h-5 w-5" /></>}</button></form><p className="mt-6 text-center text-sm text-gray-600">Already have an account? <Link className="font-semibold text-blue-700" to="/login">Sign in here</Link></p></main><Toaster position="top-right" /></div>;
}
