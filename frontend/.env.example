# ─── API ─────────────────────────────────────────────────────────────────────
# URL of the running NestJS backend
# Development default — change to your deployed backend URL in production
VITE_API_URL=http://localhost:3000
